---
description: Concept UI design — switch to Kimi K2, emit ready HTML
argument-hint: "<description>"
---
UI concept design task: $@

First: if current model is NOT `moonshotai/kimi-k2.7-code`, stop and tell me to switch (Ctrl+P → Kimi K2) before proceeding — Kimi is the design model.

Then produce a CONCEPT design, not production code:
- Single self-contained HTML file (inline CSS, no build step) that renders standalone in a browser.
- Focus on visual concept: layout, hierarchy, typography, spacing, color. Aesthetic over logic.
- Use placeholder data/content. No backend, no real logic.
- Modern, distinctive look — avoid generic template feel.

Output the full HTML in one code block, then a 3-line note on the design direction.
