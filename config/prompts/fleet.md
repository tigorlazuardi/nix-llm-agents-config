---
description: Fleet — derive and validate XL execution graph
argument-hint: "<approved spec/ticket refs>"
---
Plan Fleet for: $@

Load `~/.pi/agent/skills/fleet-plan/SKILL.md`; follow its workflow exactly. Complete with validated Fleet state plus graph/evidence pointer for explicit human approval. Defer execution to `/captain`; otherwise return terminal blocked evidence.
