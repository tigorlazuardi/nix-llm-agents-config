diff --git a/src/monitor-ws.ts b/src/monitor-ws.ts
index 8237a88..8dd62ed 100644
--- a/src/monitor-ws.ts
+++ b/src/monitor-ws.ts
@@ -11,8 +11,8 @@
  * an actionable error rather than crashing.
  */
 
-import { closeSync, mkdirSync, openSync, writeSync } from "node:fs";
-import { dirname } from "node:path";
+import { closeSync, writeSync } from "node:fs";
+import { openPrivateLog } from "./spawn.ts";
 
 export interface WsSpec {
     url: string;
@@ -52,8 +52,7 @@ export function openWsSource(spec: WsSpec, logPath: string): WsSource {
     // the session, so LOG_DIR may not have been created by a spawn yet), then
     // hold one appendable fd open for the socket's lifetime — one writeSync per
     // frame instead of an open/write/close trio.
-    mkdirSync(dirname(logPath), { recursive: true });
-    const logFd = openSync(logPath, "w");
+    const logFd = openPrivateLog(logPath);
     let fdClosed = false;
     const closeFd = (): void => {
         if (fdClosed) return;
diff --git a/src/registry.ts b/src/registry.ts
index c7061af..3c9b1a8 100644
--- a/src/registry.ts
+++ b/src/registry.ts
@@ -7,6 +7,8 @@
  */
 
 import { statSync, unlinkSync } from "node:fs";
+import { tmpdir } from "node:os";
+import { join } from "node:path";
 import { formatDuration, jobLabel } from "./format.ts";
 import {
     MAX_CONCURRENT_JOBS,
@@ -27,7 +29,7 @@ export function nextJobId(reg: BackgroundRegistry): string {
 
 /** Dedicated log directory. Keeping logs in their own dir (not loose in /tmp)
  *  keeps the stale-log sweep bounded — it lists only our files. */
-export const LOG_DIR = "/tmp/pi-bg";
+export const LOG_DIR = join(tmpdir(), `pi-bg-${process.getuid?.() ?? 0}`);
 
 export function logPathFor(jobId: string): string {
     return `${LOG_DIR}/${jobId}.log`;
diff --git a/src/spawn.ts b/src/spawn.ts
index 509af31..f913305 100644
--- a/src/spawn.ts
+++ b/src/spawn.ts
@@ -1,8 +1,39 @@
 // src/spawn.ts
 import { spawn } from "node:child_process";
-import { closeSync, mkdirSync, openSync, unlinkSync } from "node:fs";
+import {
+    chmodSync,
+    closeSync,
+    constants,
+    fchmodSync,
+    lstatSync,
+    mkdirSync,
+    openSync,
+    unlinkSync,
+} from "node:fs";
 import { dirname } from "node:path";
 
+const uid = process.getuid?.();
+
+export function ensurePrivateDirectory(path: string): void {
+    mkdirSync(path, { recursive: true, mode: 0o700 });
+    const before = lstatSync(path);
+    if (!before.isDirectory() || before.isSymbolicLink() || (uid !== undefined && before.uid !== uid)) {
+        throw new Error("Background-task directory failed ownership or type check");
+    }
+    chmodSync(path, 0o700);
+}
+
+export function openPrivateLog(path: string): number {
+    ensurePrivateDirectory(dirname(path));
+    const fd = openSync(
+        path,
+        constants.O_WRONLY | constants.O_CREAT | constants.O_TRUNC | constants.O_NOFOLLOW,
+        0o600
+    );
+    fchmodSync(fd, 0o600);
+    return fd;
+}
+
 export interface SpawnResult {
     pid: number;
     logPath: string;
@@ -30,11 +61,10 @@ export function spawnWithFileOutput(args: {
     errPath?: string;
     signal?: AbortSignal;
 }): SpawnResult {
-    mkdirSync(dirname(args.logPath), { recursive: true });
-    const outFd = openSync(args.logPath, "w");
+    const outFd = openPrivateLog(args.logPath);
     let errFd: number;
     try {
-        errFd = args.errPath ? openSync(args.errPath, "w") : outFd;
+        errFd = args.errPath ? openPrivateLog(args.errPath) : outFd;
     } catch (err) {
         closeSync(outFd);
         throw err;
diff --git a/src/tools/agent-bg.ts b/src/tools/agent-bg.ts
index 7326f6f..42042fc 100644
--- a/src/tools/agent-bg.ts
+++ b/src/tools/agent-bg.ts
@@ -8,8 +8,9 @@
  */
 
 import { execSync } from "node:child_process";
+import { randomBytes } from "node:crypto";
 import { unlinkSync, writeFileSync } from "node:fs";
-import { tmpdir } from "node:os";
+import { join } from "node:path";
 import type { ExtensionAPI, SessionEntry } from "@earendil-works/pi-coding-agent";
 import { Type } from "@earendil-works/pi-ai";
 import type { BackgroundRegistry } from "../state.ts";
@@ -19,8 +20,8 @@ import {
     requireExistingCwd as requireCwd,
     startBackgroundJob,
 } from "../lifecycle.ts";
-import { add, createRunningJob, nextJobId, logPathFor } from "../registry.ts";
-import { spawnWithFileOutput, type SpawnResult } from "../spawn.ts";
+import { add, createRunningJob, LOG_DIR, nextJobId, logPathFor } from "../registry.ts";
+import { ensurePrivateDirectory, spawnWithFileOutput, type SpawnResult } from "../spawn.ts";
 import { streamLog } from "../output.ts";
 import { textBlock } from "../format.ts";
 
@@ -117,8 +118,9 @@ export function registerAgentBgTool(pi: ExtensionAPI, reg: BackgroundRegistry):
                 "", "Continue from where you left off.",
             ].join("\n");
 
-            const promptFile = `${tmpdir()}/pi-bg-prompt-${id}.md`;
-            writeFileSync(promptFile, promptContent);
+            ensurePrivateDirectory(LOG_DIR);
+            const promptFile = join(LOG_DIR, `prompt-${id}-${randomBytes(8).toString("hex")}.md`);
+            writeFileSync(promptFile, promptContent, { flag: "wx", mode: 0o600 });
 
             const model = ctx.model;
             const modelArg = model ? `${model.provider}/${model.id}` : undefined;
