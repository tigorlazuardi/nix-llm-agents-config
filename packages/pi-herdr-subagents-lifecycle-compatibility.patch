--- a/pi-extension/subagents/herdr.ts
+++ b/pi-extension/subagents/herdr.ts
@@ -249,8 +249,13 @@
   herdrExec(["pane", "run", surface, command]);
 }

+export function buildHerdrEscapeArgs(surface: string): string[] {
+  // First Escape leaves Vim insert mode; second cancels the active Pi turn.
+  return ["pane", "send-keys", surface, "Escape", "Escape"];
+}
+
 export function sendHerdrEscape(surface: string): void {
-  herdrExec(["pane", "send-keys", surface, "Escape"]);
+  herdrExec(buildHerdrEscapeArgs(surface));
 }

 export function closeHerdrSurface(surface: string): void {
--- a/pi-extension/subagents/subagent-done.ts
+++ b/pi-extension/subagents/subagent-done.ts
@@ -1,14 +1,16 @@
 /**
  * Extension loaded into sub-agents.
- * - Shows agent identity + available tools as a styled widget above the editor (toggle with Ctrl+J)
+ * - Shows agent identity + active tools as a styled widget above the editor
  * - Provides a `subagent_done` tool for autonomous agents to self-terminate
  */
-import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
+import { rawKeyHint, type ExtensionAPI } from "@earendil-works/pi-coding-agent";
 import { Box, Text } from "@earendil-works/pi-tui";
 import { Type } from "@sinclair/typebox";
 import { writeFileSync } from "node:fs";
 import { createSubagentActivityRecorder } from "./activity.ts";

+const TOGGLE_SHORTCUT = "ctrl+shift+o";
+
 export function shouldMarkUserTookOver(agentStarted: boolean): boolean {
   return agentStarted;
 }
@@ -109,7 +111,7 @@
         if (expanded) {
           // Expanded: full tool list + denied
           const countInfo = theme.fg("dim", ` — ${toolNames.length} available`);
-          const hint = theme.fg("muted", "  (Ctrl+J to collapse)");
+          const hint = theme.fg("muted", `  (${rawKeyHint(TOGGLE_SHORTCUT, "to collapse")})`);

           const toolList = toolNames
             .map((name: string) => theme.fg("dim", name))
@@ -136,7 +138,7 @@
             denied.length > 0
               ? theme.fg("dim", " · ") + theme.fg("error", `${denied.length} denied`)
               : "";
-          const hint = theme.fg("muted", "  (Ctrl+J to expand)");
+          const hint = theme.fg("muted", `  (${rawKeyHint(TOGGLE_SHORTCUT, "to expand")})`);

           const content = new Text(`${agentTag}${countInfo}${deniedInfo}${hint}`, 0, 0);
           box.addChild(content);
@@ -155,8 +157,7 @@
   // Show widget + status bar on session start
   pi.on("session_start", (_event, ctx) => {
     recorder.sessionStart();
-    const tools = pi.getAllTools();
-    toolNames = tools.map((t) => t.name).sort();
+    toolNames = pi.getActiveTools().sort();
     denied = parseDeniedTools(deniedToolsValue);

     renderWidget(ctx, null);
@@ -264,8 +265,7 @@
     recorder.sessionShutdown((event as any).reason);
   });

-  // Toggle expand/collapse with Ctrl+J
-  pi.registerShortcut("ctrl+j", {
+  pi.registerShortcut(TOGGLE_SHORTCUT, {
     description: "Toggle subagent tools widget",
     handler: (ctx) => {
       expanded = !expanded;
--- a/pi-extension/subagents/index.ts
+++ b/pi-extension/subagents/index.ts
@@ -1357,6 +1357,10 @@
     envParts.push(`PI_CODING_AGENT_DIR=${shellQuote(process.env.PI_CODING_AGENT_DIR)}`);
   }

+  if (toolAllowlist) {
+    envParts.push(`PI_SUBAGENT_ALLOWED_TOOLS=${shellQuote(toolAllowlist)}`);
+  }
+
   if (denySet.size > 0) {
     envParts.push(`PI_DENY_TOOLS=${shellQuote([...denySet].join(","))}`);
   }
